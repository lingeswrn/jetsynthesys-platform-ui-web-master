'use strict'
import chai from 'chai'

// REVIEW:
//      - http://chaijs.com/plugins/

const expect = chai.expect
const assert = chai.assert

describe('Test', () => {
    describe('example',  () => {
        it('should be true',  () => {
            expect(1).to.equal(1)
        })
    })
})
